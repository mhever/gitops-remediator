# Create this secret manually — do NOT commit real values.
# kubectl create secret generic gitops-remediator-secrets \
#   --namespace=remediator-system \
#   --from-literal=deepseek-api-key=<your-key> \
#   --from-literal=github-token=<your-token>
apiVersion: v1
kind: Secret
metadata:
  name: gitops-remediator-secrets
  namespace: remediator-system
type: Opaque
stringData:
  deepseek-api-key: REPLACE_ME
  github-token: REPLACE_ME
