apiVersion: v1
kind: Service
metadata:
  name: gitops-remediator-metrics
  namespace: remediator-system
  labels:
    app: gitops-remediator
spec:
  selector:
    app: gitops-remediator
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
